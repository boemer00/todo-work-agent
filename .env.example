# OpenAI API Configuration
OPENAI_API_KEY=sk-your-openai-api-key-here

# LangSmith Configuration (Optional - for observability)
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=your-langsmith-api-key-here
LANGCHAIN_PROJECT=my-agent

# Twilio WhatsApp Configuration
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your-twilio-auth-token-here
TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886

# Security: Skip webhook signature verification (ONLY for local development)
# WARNING: Never set to true in production - this bypasses security checks
# SKIP_WEBHOOK_VERIFICATION=true

# Redis Cloud Configuration (for rate limiting)
# Get this from https://redis.io/try-free/ (free tier: 30MB)
# Format: rediss://default:PASSWORD@redis-12345.cloud.redislabs.com:12345
# If not set, falls back to in-memory storage (not recommended for production with multiple instances)
# REDIS_URL=rediss://default:your_password_here@redis-xxxxx.cloud.redislabs.com:xxxxx

# Google Cloud Configuration (for Cloud Run deployment)
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_STORAGE_BUCKET=your-bucket-name
CLOUD_RUN=false

# Google Calendar Service Account (for Cloud Run)
# The name of the secret in Google Cloud Secret Manager containing the service account key JSON
# See SERVICE_ACCOUNT_SETUP.md for setup instructions
CALENDAR_SERVICE_ACCOUNT_SECRET=calendar-service-account

# Database Configuration (optional - defaults to local)
DATABASE_PATH=data/tasks.db

# Timezone Configuration (for date parsing and display)
# Use timezone names from IANA database: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
# Examples: Europe/London, America/New_York, Asia/Tokyo
# Default: Europe/London
DEFAULT_TIMEZONE=Europe/London
